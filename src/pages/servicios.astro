---
import Layout from '../layouts/Layout.astro';
import { Icon } from 'astro-icon/components';
---
<Layout 
    title="Servicios - Transmisiones Automáticas SV"
    description="Servicios completos de transmisiones automáticas. Venta, instalación, diagnóstico y mantenimiento en El Salvador."
>
    <!-- Glass Hero -->
    <section class="hero-glass">
        <div class="glass-background"></div>
        <div class="hero-content animate-on-scroll">
            <h1 class="hero-title">Nuestros Servicios</h1>
            <p class="hero-subtitle">Soluciones completas para tu transmisión automática</p>
        </div>
    </section>

    <!-- Services Grid -->
    <main class="content-container">
        <div class="services-grid">
            <div class="service-card animate-on-scroll">
                <div class="service-icon">
                    <Icon name="mdi:cogs" />
                </div>
                <h3>Venta de Transmisiones</h3>
                <p>Catálogo completo de transmisiones nuevas, usadas y reconstruidas con hasta 12 meses de garantía extendida.</p>
                <div class="service-features">
                    <span>Garantía extendida hasta 12 meses</span>
                    <span>Todas las marcas disponibles</span>
                    <span>Precios competitivos</span>
                </div>
            </div>

            <div class="service-card animate-on-scroll">
                <div class="service-icon">
                    <Icon name="mdi:wrench" />
                </div>
                <h3>Instalación Profesional</h3>
                <p>Técnicos certificados garantizan instalación precisa y rendimiento óptimo de tu nueva transmisión.</p>
                <div class="service-features">
                    <span>Técnicos certificados ASE</span>
                    <span>Instalación con garantía</span>
                    <span>Pruebas de calidad</span>
                </div>
            </div>

            <div class="service-card animate-on-scroll">
                <div class="service-icon">
                    <Icon name="mdi:magnify-scan" />
                </div>
                <h3>Diagnóstico por Computadora</h3>
                <p>Tecnología de punta para diagnóstico exacto y preciso de fallas en tu sistema de transmisión automática.</p>
                <div class="service-features">
                    <span>Diagnóstico computarizado</span>
                    <span>Equipos de última generación</span>
                    <span>Reporte detallado</span>
                </div>
            </div>

            <div class="service-card animate-on-scroll">
                <div class="service-icon">
                    <Icon name="mdi:oil" />
                </div>
                <h3>Mantenimiento Preventivo</h3>
                <p>Cambio de aceite y filtro esencial para prolongar significativamente la vida útil de tu transmisión.</p>
                <div class="service-features">
                    <span>Mantenimiento programado</span>
                    <span>Aceites premium certificados</span>
                    <span>Filtros originales</span>
                </div>
            </div>

            <div class="service-card animate-on-scroll">
                <div class="service-icon">
                    <Icon name="mdi:home-search" />
                </div>
                <h3>Diagnóstico a Domicilio</h3>
                <p>Servicio premium de diagnóstico directamente en tu hogar u oficina para tu máxima comodidad.</p>
                <div class="service-features">
                    <span>Servicio en tu ubicación</span>
                    <span>Equipos portátiles profesionales</span>
                    <span>Sin costo de movilización</span>
                </div>
            </div>

            <div class="service-card animate-on-scroll">
                <div class="service-icon">
                    <Icon name="mdi:truck-delivery" />
                </div>
                <h3>Reparaciones a Domicilio</h3>
                <p>Reparaciones menores y mantenimiento realizado directamente donde te encuentres, ahorrándote tiempo.</p>
                <div class="service-features">
                    <span>Reparaciones in-situ</span>
                    <span>Mantenimiento móvil</span>
                    <span>Disponibilidad flexible</span>
                </div>
            </div>
        </div>

        <!-- CTA Section -->
        <section class="cta-section animate-on-scroll">
            <h2>¿Listo para mejorar tu vehículo?</h2>
            <a href="/contacto" class="cta-button">
                <span>Solicitar Cotización</span>
                <Icon name="mdi:arrow-right" />
            </a>
        </section>
    </main>
</Layout>

<style>
    /* ===== PREMIUM SERVICIOS HERO ===== */
    .hero-glass {
        position: relative;
        min-height: 70vh;
        display: flex;
        align-items: center;
        justify-content: center;
        text-align: center;
        background: linear-gradient(135deg, 
            var(--bg-900) 0%, 
            var(--bg-800) 40%, 
            var(--bg-700) 100%);
        overflow: hidden;
        padding: var(--spacing-3xl) 0;
    }

    .glass-background {
        position: absolute;
        inset: 0;
        background: 
            radial-gradient(circle at 25% 75%, rgba(255,107,26,0.15) 0%, transparent 50%),
            radial-gradient(circle at 75% 25%, rgba(0,212,255,0.08) 0%, transparent 50%),
            radial-gradient(circle at 50% 50%, rgba(255,215,0,0.06) 0%, transparent 40%);
        animation: backgroundFloat 20s ease-in-out infinite;
    }

    @keyframes backgroundFloat {
        0%, 100% { 
            transform: translateY(0) rotate(0deg) scale(1);
            opacity: 0.8;
        }
        50% { 
            transform: translateY(-20px) rotate(1deg) scale(1.1);
            opacity: 1;
        }
    }

    .hero-content {
        position: relative;
        z-index: 2;
        padding: var(--spacing-3xl);
        max-width: 900px;
        margin: 0 auto;
        background: var(--glass-strong);
        backdrop-filter: blur(24px) saturate(1.4);
        border-radius: var(--radius-2xl);
        border: 1px solid var(--glass-border);
        box-shadow: var(--shadow-2xl), var(--glow-primary);
        animation: heroSlideIn 1.2s var(--ease-out) both;
    }

    @keyframes heroSlideIn {
        from {
            opacity: 0;
            transform: translateY(50px) scale(0.95);
        }
        to {
            opacity: 1;
            transform: translateY(0) scale(1);
        }
    }

    .hero-title {
        font-size: clamp(2.5rem, 8vw, 4.5rem);
        font-weight: 800;
        background: var(--gradient-primary);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        text-shadow: var(--glow-primary);
        margin-bottom: var(--spacing-lg);
        font-family: 'Orbitron', sans-serif;
        letter-spacing: -0.02em;
        animation: titleGlow 3s ease-in-out infinite;
    }

    @keyframes titleGlow {
        0%, 100% { 
            text-shadow: var(--glow-primary); 
        }
        50% { 
            text-shadow: 
                var(--glow-primary),
                0 0 40px rgba(255,107,26,0.5),
                0 0 80px rgba(255,107,26,0.3); 
        }
    }

    .hero-subtitle {
        font-size: clamp(1.1rem, 3vw, 1.4rem);
        color: var(--text-secondary);
        line-height: 1.7;
        animation: subtitleFadeIn 1.5s var(--ease-out) 0.3s both;
    }

    @keyframes subtitleFadeIn {
        from {
            opacity: 0;
            transform: translateY(30px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    /* ===== PREMIUM CONTENT CONTAINER ===== */
    .content-container {
        padding: var(--spacing-3xl) var(--spacing-md);
        max-width: 1400px;
        margin: 0 auto;
    }

    /* ===== PREMIUM SERVICES GRID ===== */
    .services-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(380px, 1fr));
        gap: var(--spacing-2xl);
        margin-bottom: var(--spacing-4xl);
    }

    .service-card {
        position: relative;
        background: var(--glass-medium);
        backdrop-filter: blur(20px) saturate(1.3);
        border: 1px solid var(--glass-border);
        border-radius: var(--radius-2xl);
        padding: var(--spacing-3xl);
        text-align: center;
        transition: all var(--motion-medium) var(--ease-out);
        overflow: hidden;
        box-shadow: var(--shadow-lg);
        animation: serviceSlideIn 1s var(--ease-out) both;
    }

    .service-card:nth-child(1) { animation-delay: 0.2s; }
    .service-card:nth-child(2) { animation-delay: 0.4s; }
    .service-card:nth-child(3) { animation-delay: 0.6s; }
    .service-card:nth-child(4) { animation-delay: 0.8s; }
    .service-card:nth-child(5) { animation-delay: 1s; }
    .service-card:nth-child(6) { animation-delay: 1.2s; }

    @keyframes serviceSlideIn {
        from {
            opacity: 0;
            transform: translateY(50px) scale(0.9);
        }
        to {
            opacity: 1;
            transform: translateY(0) scale(1);
        }
    }

    .service-card::before {
        content: '';
        position: absolute;
        inset: 0;
        background: linear-gradient(135deg, 
            rgba(255,107,26,0.1) 0%, 
            transparent 50%, 
            rgba(0,212,255,0.05) 100%);
        opacity: 0;
        transition: opacity var(--motion-medium) var(--ease-out);
        border-radius: var(--radius-2xl);
    }

    .service-card:hover {
        transform: translateY(-15px) scale(1.03);
        border-color: var(--border-accent);
        box-shadow: var(--shadow-2xl), var(--glow-primary);
    }

    .service-card:hover::before {
        opacity: 1;
    }

    .service-icon {
        font-size: 4rem;
        color: var(--primary);
        margin-bottom: var(--spacing-xl);
        padding: var(--spacing-xl);
        background: var(--glass-light);
        border-radius: var(--radius-xl);
        border: 1px solid var(--glass-border);
        box-shadow: var(--glow-primary);
        display: inline-flex;
        align-items: center;
        justify-content: center;
        transition: all var(--motion-medium) var(--ease-out);
    }

    .service-card:hover .service-icon {
        transform: scale(1.2) rotate(10deg);
        box-shadow: var(--glow-secondary);
    }

    .service-card h3 {
        font-size: clamp(1.3rem, 3vw, 1.8rem);
        font-weight: 700;
        color: var(--text-primary);
        margin-bottom: var(--spacing-lg);
        font-family: 'Orbitron', sans-serif;
        background: var(--gradient-primary);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        letter-spacing: -0.01em;
    }

    .service-card p {
        color: var(--text-secondary);
        line-height: 1.8;
        margin-bottom: var(--spacing-xl);
        font-size: 1.05rem;
    }

    .service-features {
        display: flex;
        flex-direction: column;
        gap: var(--spacing-sm);
    }

    .service-features span {
        color: var(--text-muted);
        font-size: 0.95rem;
        padding: var(--spacing-sm) var(--spacing-md);
        background: var(--glass-light);
        border-radius: var(--radius-md);
        border: 1px solid var(--glass-border);
        transition: all var(--motion-fast) var(--ease-out);
        position: relative;
        overflow: hidden;
    }

    .service-features span::before {
        content: '✓';
        color: var(--primary);
        font-weight: 800;
        margin-right: var(--spacing-sm);
        font-size: 1.1rem;
    }

    .service-features span::after {
        content: '';
        position: absolute;
        inset: 0;
        background: linear-gradient(135deg, 
            rgba(255,107,26,0.1) 0%, 
            transparent 100%);
        opacity: 0;
        transition: opacity var(--motion-fast) var(--ease-out);
    }

    .service-card:hover .service-features span::after {
        opacity: 1;
    }

    /* ===== PREMIUM CTA SECTION ===== */
    .cta-section {
        text-align: center;
        padding: var(--spacing-4xl) 0;
        background: var(--glass-light);
        backdrop-filter: blur(16px) saturate(1.2);
        border-radius: var(--radius-2xl);
        border: 1px solid var(--glass-border);
        box-shadow: var(--shadow-lg);
        animation: ctaSlideIn 1s var(--ease-out) 1.4s both;
    }

    @keyframes ctaSlideIn {
        from {
            opacity: 0;
            transform: translateY(40px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    .cta-section h2 {
        font-size: clamp(1.8rem, 4vw, 2.5rem);
        font-weight: 700;
        color: var(--text-primary);
        margin-bottom: var(--spacing-2xl);
        font-family: 'Orbitron', sans-serif;
        background: var(--gradient-primary);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        letter-spacing: -0.01em;
    }

    .cta-button {
        position: relative;
        display: inline-flex;
        align-items: center;
        gap: var(--spacing-md);
        background: var(--gradient-primary);
        color: var(--bg-900);
        padding: var(--spacing-lg) var(--spacing-2xl);
        border-radius: var(--radius-full);
        text-decoration: none;
        font-weight: 700;
        font-size: 1.1rem;
        transition: all var(--motion-medium) var(--ease-out);
        box-shadow: var(--shadow-lg), var(--glow-primary);
        overflow: hidden;
        font-family: 'Orbitron', sans-serif;
        letter-spacing: 0.01em;
    }

    .cta-button::after {
        content: '';
        position: absolute;
        inset: 0;
        background: linear-gradient(45deg, 
            transparent 30%, 
            rgba(255,255,255,0.3) 50%, 
            transparent 70%);
        transform: translateX(-100%) skewX(-15deg);
        transition: transform var(--motion-slow) var(--ease-out);
    }

    .cta-button:hover {
        transform: translateY(-8px) scale(1.05);
        box-shadow: var(--shadow-2xl), var(--glow-secondary);
    }

    .cta-button:hover::after {
        transform: translateX(200%) skewX(-15deg);
    }

    .cta-button :global(.astro-icon) {
        transition: transform var(--motion-medium) var(--ease-bounce);
    }

    .cta-button:hover :global(.astro-icon) {
        transform: translateX(8px);
    }

    /* ===== SCROLL ANIMATIONS ===== */
    .animate-on-scroll {
        opacity: 0;
        transform: translateY(50px);
        transition: all var(--motion-slow) var(--ease-out);
    }

    .animate-on-scroll.animate {
        opacity: 1;
        transform: translateY(0);
    }

    /* ===== RESPONSIVE OPTIMIZATIONS ===== */
    @media (max-width: 1024px) {
        .services-grid {
            grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
            gap: var(--spacing-xl);
        }
    }

    @media (max-width: 768px) {
        .hero-glass {
            min-height: 60vh;
            padding: var(--spacing-2xl) 0;
        }
        
        .hero-content {
            padding: var(--spacing-2xl);
            margin: 0 var(--spacing-md);
        }
        
        .content-container {
            padding: var(--spacing-2xl) var(--spacing-md);
        }
        
        .services-grid {
            grid-template-columns: 1fr;
            gap: var(--spacing-lg);
        }
        
        .service-card {
            padding: var(--spacing-2xl);
        }

        .service-icon {
            font-size: 3.5rem;
            padding: var(--spacing-lg);
        }

        .cta-button {
            padding: var(--spacing-md) var(--spacing-xl);
            font-size: 1rem;
        }
    }

    @media (max-width: 480px) {
        .hero-content {
            padding: var(--spacing-xl);
            margin: 0 var(--spacing-sm);
        }

        .service-card {
            padding: var(--spacing-xl);
        }

        .service-icon {
            font-size: 3rem;
            padding: var(--spacing-md);
        }

        .cta-button {
            padding: var(--spacing-sm) var(--spacing-lg);
            font-size: 0.95rem;
        }
    }

    /* ===== ACCESIBILIDAD Y PREFERENCIAS ===== */
    @media (prefers-reduced-motion: reduce) {
        .glass-background,
        .hero-content,
        .service-card,
        .cta-section,
        .animate-on-scroll,
        .hero-title {
            animation: none;
            transition: none;
        }
        
        .animate-on-scroll {
            opacity: 1;
            transform: none;
        }

        .service-card:hover {
            transform: translateY(-8px);
        }

        .cta-button:hover {
            transform: translateY(-4px);
        }
    }

    /* ===== PERFORMANCE OPTIMIZATIONS ===== */
    .glass-background {
        will-change: transform;
        transform: translateZ(0);
    }

    .service-card {
        will-change: transform;
    }
</style>

<script>
// ===== PREMIUM SERVICIOS EFFECTS =====

// ===== SCROLL ANIMATIONS =====
function setupScrollAnimations() {
    const animatedElements = document.querySelectorAll('.animate-on-scroll');
    
    const scrollObserver = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                entry.target.classList.add('animate');
                scrollObserver.unobserve(entry.target);
            }
        });
    }, {
        threshold: 0.1,
        rootMargin: '0px 0px -50px 0px'
    });

    animatedElements.forEach(element => {
        scrollObserver.observe(element);
    });
}

// ===== HERO PARALLAX EFFECTS =====
function setupHeroParallax() {
    const hero = document.querySelector('.hero-glass') as HTMLElement | null;
    const glassBackground = document.querySelector('.glass-background') as HTMLElement | null;
    
    if (!hero || !glassBackground) return;

    let ticking = false;
    
    const handleScroll = () => {
        if (!ticking) {
            requestAnimationFrame(() => {
                const scrollY = window.scrollY;
                const heroHeight = hero.offsetHeight;
                const scrollPercent = Math.min(scrollY / heroHeight, 1);
                
                // Parallax effect for background
                const translateY = scrollY * 0.5;
                if (glassBackground) glassBackground.style.transform = `translateY(${translateY}px)`;
                
                // Opacity fade effect
                const opacity = Math.max(0.3, 1 - scrollPercent * 0.7);
                if (hero) hero.style.opacity = opacity.toString();
                
                ticking = false;
            });
            ticking = true;
        }
    };

    window.addEventListener('scroll', handleScroll, { passive: true });
}

// ===== RIPPLE EFFECTS =====
function setupRippleEffects() {
    const cards = document.querySelectorAll('.service-card, .cta-button');
    
    cards.forEach((card) => {
        card.addEventListener('click', function (e) {
            createRipple(e as MouseEvent, card as HTMLElement);
        });
    });
}

function createRipple(event: MouseEvent, element: HTMLElement) {
    const rect = element.getBoundingClientRect();
    const size = Math.max(rect.width, rect.height);
    const x = event.clientX - rect.left - size / 2;
    const y = event.clientY - rect.top - size / 2;
    
    const ripple = document.createElement('span');
    ripple.className = 'ripple-effect';
    ripple.style.cssText = `
        position: absolute;
        width: ${size}px;
        height: ${size}px;
        left: ${x}px;
        top: ${y}px;
        background: radial-gradient(circle, rgba(255,107,26,0.3) 0%, transparent 70%);
        border-radius: 50%;
        transform: scale(0);
        animation: rippleAnimation 0.6s ease-out;
        pointer-events: none;
        z-index: 1;
    `;
    
    // Ensure relative positioning
    if (getComputedStyle(element).position === 'static') {
        element.style.position = 'relative';
    }
    
    element.appendChild(ripple);
    
    // Remove ripple after animation
    setTimeout(() => {
        if (ripple.parentNode) {
            ripple.parentNode.removeChild(ripple);
        }
    }, 600);
}

// ===== CARD HOVER EFFECTS =====
function setupCardEffects() {
    const serviceCards = document.querySelectorAll('.service-card');
    
    serviceCards.forEach((card) => {
        // Add 3D tilt effect on mouse move
        card.addEventListener('mousemove', function (e) {
            const rect = (card as HTMLElement).getBoundingClientRect();
            const { clientX, clientY } = e as any;
            const x = clientX - rect.left;
            const y = clientY - rect.top;
            
            const centerX = rect.width / 2;
            const centerY = rect.height / 2;
            
            const rotateX = (y - centerY) / 20;
            const rotateY = (centerX - x) / 20;
            
            (card as HTMLElement).style.transform = `
                perspective(1000px) 
                rotateX(${rotateX}deg) 
                rotateY(${rotateY}deg) 
                translateY(-15px) 
                scale(1.03)
            `;
        });
        
        // Reset transform on mouse leave
        card.addEventListener('mouseleave', function () {
            (card as HTMLElement).style.transform = 'perspective(1000px) rotateX(0) rotateY(0) translateY(0) scale(1)';
        });
    });
}

// ===== RIPPLE ANIMATION STYLES =====
const style = document.createElement('style');
style.textContent = `
    @keyframes rippleAnimation {
        from {
            transform: scale(0);
            opacity: 1;
        }
        to {
            transform: scale(2);
            opacity: 0;
        }
    }
`;
document.head.appendChild(style);

// ===== INITIALIZATION =====
document.addEventListener('DOMContentLoaded', () => {
    setupScrollAnimations();
    setupHeroParallax();
    setupRippleEffects();
    setupCardEffects();
});
</script>